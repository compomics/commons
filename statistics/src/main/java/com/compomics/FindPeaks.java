package com.compomics;

import java.util.Comparator;
import java.util.List;

/**
 * Created by Davy Maddelein on 06/10/2015.
 */
public class FindPeaks {

    /**
     * finds all points in a 2D space before a downwards trend
     * @param graph
     * @return
     */
/**    public static List<Tuple> findPeaks(List<Tuple> graph){
        graph.stream().sorted(Comparator.comparingDouble(value -> value.getX().doubleValue()))
                .forEach();
    }
*/
}
